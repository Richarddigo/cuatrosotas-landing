// This file is intentionally empty.
// The middleware (localePrefix: 'as-needed') serves the default locale at /
// without a redirect, so this fallback page is no longer needed.
export default function RootPage() {
  return null;
}
